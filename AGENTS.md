我们要从零实现一个 GPU 故障诊断 Agent。

项目背景：
在边缘计算/云计算运维场景中，监控系统发现某台 GPU 机器异常后，
传统方式是运维人员登录机器，根据经验执行 nvidia-smi、dmesg、
查看进程、驱动状态、GPU Xid 等信息，一步一步排查故障。

这个项目希望把人工诊断流程 Agent 化：

告警输入
    ↓
Agent 分析当前信息
    ↓
选择诊断工具
    ↓
执行工具获取机器状态
    ↓
根据 Observation 决定下一步
    ↓
重复诊断
    ↓
输出故障原因、证据链和处理建议

当前项目主要用于个人学习、秋招项目展示，不接入真实百度内部系统。
第一版应当能够在本地完整运行，可以通过 Mock/模拟故障机器构造不同 GPU 故障场景。

我倾向使用 Go 实现。

我希望自己掌握项目整体架构和核心原理，而不是让你直接生成一个我看不懂的完整项目。

所以现在：
1. 不要写代码。
2. 分析这个需求。
3. 找出当前需求中尚未明确、但会影响架构的重要问题。
4. 给出 2~3 种合理架构方案以及各自 trade-off。
5. 给出你认为最适合作为秋招项目的 MVP。
6. 明确哪些设计决策必须由我理解和决定，哪些实现细节可以交给你完成。

---

## 当前讨论结论（后续会话的重要上下文）

以下内容是当前阶段形成的初步共识，用于指导后续需求分析。它是一套面向本地 Mock Demo 的设计，不代表可以直接用于生产环境；真实生产责任边界仍需由在职运维、业务负责人和安全人员共同确认。

### 1. 产品定位与主要用户

项目的主要用户应当是刚开始值班、不熟悉 GPU 告警优先级和团队 SOP 的新人运维，而不是试图用 AI 替代资深运维。

更准确的产品定位是：

> 面向新人值班运维的 GPU 告警诊断与 SOP 执行 Agent。系统对常见告警匹配经过审核的标准流程，自动完成低风险信息采集，结合现场 Observation 逐步解释和指导；对未知、证据冲突或无法确认的问题，整理上下文并升级资深运维。

不同角色的职责：

- 新人运维：主要使用者，获得告警分级、SOP 引导、结果解释和升级建议。
- 资深运维：提供和审核 SOP，处理未知故障，决定高风险操作。
- Agent：辅助执行 SOP、采集信息、解释结果、整理未知问题，不冒充资深运维独立处置所有故障。
- 外部策略引擎：决定某个操作可以自动执行、需要人工确认，还是禁止执行。
- 工具系统：真正读取或改变机器状态。

资深运维不是主要用户，但可以通过自动信息采集、结构化证据、交接摘要和知识沉淀减少重复劳动。

### 2. 常见问题与未知问题的处理方式

常见 GPU 告警通常已有固定 SOP，不应为了体现“Agent”而让大模型重新发明流程。告警与 SOP 的初始匹配应由确定性的路由器完成，概念上类似：

```text
告警类型 + 严重级别 + 持续时间 + 环境 + 影响范围
→ 已审核的结构化 SOP
```

已知问题采用“SOP 主导、Agent 辅助”的方式：

- SOP 决定必须执行的步骤、条件和分支。
- Agent 帮助新人理解步骤、调用允许的诊断工具、解释 Observation，并记录当前进度。
- Agent 不得擅自修改 SOP、跳过强制步骤或把自己的推测冒充团队规定。

未知问题不要求 Agent 冒充老员工独立找出根因。默认流程是：

```text
无法匹配 SOP / SOP 无法解释现象 / 证据冲突
→ 执行通用的只读基线检查
→ Agent 汇总事实、异常和有限的候选方向
→ 生成完整交接摘要
→ 升级资深运维
```

因此当前推荐的总体调度模式是：

```text
告警输入
→ 确定性告警分级与 SOP 路由
→ 命中 SOP：按结构化流程执行，Agent 辅助解释和推进
→ 未命中或无法判断：只读检查、信息整理、升级人工
```

### 3. 权限和责任边界

权限判断必须在 Agent 外部，由确定性策略程序执行，不能依赖 Prompt 或让大模型自行批准操作。

MVP 暂定三级策略：

1. 自动执行：白名单内、只读、低负载、不改变机器状态的检查，例如查询 GPU 状态、显存、温度、进程、驱动模块和近期 Xid 日志。
2. 人工确认：会改变局部状态的操作，例如终止进程、GPU reset、重新加载驱动或隔离设备。执行前必须展示依据、影响范围、风险和可恢复性。
3. 禁止执行：重启或关闭机器、升级驱动、修改内核参数、刷写固件、删除业务数据、批量操作等。系统不提供对应执行工具，只能建议升级人工。

“客户是否有感”不是唯一判断标准，还要考虑：

- 是否改变状态。
- 是否可逆。
- 影响范围是进程、单卡、单机还是集群。
- 节点是否已经隔离。
- 操作失败的后果。
- 当前证据是否充分。

第一版主要运行在本地 Mock/沙盒环境。即使如此，也应展示“自动通过、等待人工确认、策略禁止”三种策略结果。

### 4. 自然语言 SOP 与结构化 SOP

现实中的 SOP 通常是一段给人阅读的自然语言文档，但可靠执行需要结构化流程。因此需要区分：

- 自然语言 SOP：知识来源，保留背景、经验、注意事项和原文依据。
- 结构化 SOP：经过审核的可执行产物，供确定性流程引擎使用。

建议采用类似“编译”的发布过程：

```text
自然语言 SOP
→ AI 辅助提取结构和发现歧义
→ 生成结构化 SOP 草稿
→ 资深运维审核、修改
→ 测试
→ 发布为可执行 SOP
```

AI 转换时遇到“必要时”“较高”“确认正常”等模糊表达，不能自行补全，应生成待确认问题。未经人工审核的自然语言内容不得直接成为可执行生产操作。

结构化 SOP 至少应描述：

- 唯一标识、版本、作者、审核状态和适用环境。
- 对应告警及触发条件。
- 每一步的目的、诊断工具、输入和风险等级。
- 基于结构化 Observation 的分支条件。
- 成功、无法判断、工具失败、等待确认、策略禁止等终止状态。
- 每一步对应的原始文档来源和面向新人的解释。

MVP 不需要实现完整的 SOP 自动转换平台。可以先准备 3 个自然语言 SOP，在 AI 辅助下人工转换并审核为结构化版本，用于验证运行时流程。

### 5. RAG/知识库与可执行流程的关系

RAG 适合“找到知识”，不适合“控制执行”。它可以用于：

- 根据自然语言现象查找相关 SOP。
- 检索设备资料和历史故障案例。
- 回答新人为什么需要某项检查。
- 展示答案对应的 SOP 原文。

只有检索到已经发布的结构化 SOP 时，系统才能启动可执行流程；普通文档和群聊知识只能作为参考或建议。

可持续知识沉淀可以作为后续扩展：

```text
未知告警
→ Agent 收集信息并升级老员工
→ 老员工完成诊断和回答
→ AI 生成知识条目或 SOP 草稿
→ 人工审核
→ 发布到知识库或结构化 SOP 库
```

必须区分普通知识条目和可执行 SOP。群聊中的经验回答不能未经审核直接升级为可执行操作。

### 6. Shell 命令与诊断工具

底层可以使用 Linux Shell 命令，但 SOP 和 Agent 不应直接获得任意 Shell 执行能力。

推荐分层：

```text
结构化 SOP
→ 引用语义化诊断工具
→ 工具内部执行受控 Shell 命令或返回 Mock 数据
→ 输出结构化 Observation
```

诊断工具可以是对一条命令的薄封装，其价值在于增加：

- 明确的诊断目的和语义。
- 参数校验和命令白名单。
- 权限与风险等级。
- 超时、输出大小和调用次数限制。
- 结构化结果解析。
- 审计记录。
- 真实机器实现与 Mock 实现的可替换性。

例如 SOP 调用 `query_gpu_processes`，真实实现可执行受控的 `nvidia-smi` 查询，Mock 实现则返回同一结构的模拟进程结果。

后续可以考虑一个“提出只读 Shell 命令”的逃生能力，但不能直接开放任意 Shell。候选命令需要经过外部白名单校验和人工确认。MVP 暂不需要实现该能力。

### 7. 当前建议的 MVP 范围

第一版建议验证以下核心闭环：

```text
Mock 告警
→ 告警分级
→ 匹配已审核的结构化 SOP
→ 调用只读诊断工具获取 Mock 机器状态
→ 根据 Observation 走不同分支
→ Agent 面向新人解释当前现象和下一步
→ 输出诊断结论、证据链、操作建议或升级摘要
```

可选择 3 个代表性场景：

- GPU 显存占用过高。
- GPU 温度过高。
- GPU Xid 错误或 GPU 丢卡。

另加一个正常场景和一个无法匹配 SOP 的未知场景，用于验证“不误报”和“知道何时升级人工”。

第一版应重点展示：

- 新人如何知道告警是否重要。
- 如何快速找到并执行团队认可的 SOP。
- 工具结果如何推动流程分支，而不是只展示文档。
- 最终结论如何绑定真实 Observation。
- 系统如何拦截高风险操作。
- 未知问题如何生成完整交接摘要。

### 8. 与普通问答助手的区别

普通知识问答助手主要回答“应该怎么做”；本项目还要参与完成诊断过程：读取机器状态、保存 SOP 执行进度、根据 Observation 推进分支、验证告警是否恢复，并输出可审计的证据链。

可以将整个能力链理解为：

```text
群聊和文档
→ 知识条目
→ 自然语言 SOP
→ 已审核的结构化 SOP
→ 可执行诊断流程
→ 运行结果与复盘
→ 反向补充知识库
```

MVP 聚焦后半段，即“结构化 SOP + 工具执行 + Observation 驱动流程”，避免退化成普通的 RAG 文档问答项目。

## 后续优先讨论的问题

下一个会话建议从“告警重要性如何判断”开始，因为新人首先面临的是告警分级，而不只是不会执行命令。需要进一步明确：

1. 告警输入包含哪些字段和上下文。
2. 严重性、紧急性、影响范围分别如何定义。
3. 告警分级由固定规则、历史数据还是模型辅助完成。
4. 重复告警、关联告警和告警风暴是否属于 MVP。
5. 分级结果如何决定响应时限、SOP 路由和人工升级。

之后仍需讨论的重要问题包括：

- Observation 和证据链的数据模型。
- 结构化 SOP 的最小表达能力。
- Mock 机器如何保证不同工具结果一致。
- Agent 的上下文、终止条件和防循环机制。
- MVP 的评测指标和演示方式。
