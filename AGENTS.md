# GPU 只读故障诊断 Agent 项目协作说明

## 1. 项目目标

我们要从零实现一个面向 GPU 运维场景的受控只读诊断 Agent。

在边缘计算或云计算运维场景中，监控系统发现 GPU 宿主机异常后，运维人员通常需要登录机器，根据经验执行 `nvidia-smi`、查询进程、驱动状态、内核日志和 GPU Xid 等命令，再把分散的信息关联起来判断故障。新人不熟悉命令和排查顺序；资深运维也需要重复完成大量基础信息采集。

本项目让 Agent 自动完成这段安全但重复的调查过程：

```text
Mock 告警输入
→ 匹配已知 SOP 或进入受限通用调查
→ Agent/SOP 选择受控的只读语义化工具
→ 工具读取统一的 Mock Machine State
→ 返回结构化 Observation
→ 根据 Observation 决定下一项只读检查或终止
→ 输出事实、推断、未知项、证据链和人工处理建议
```

Agent 不具备修改机器状态的权限，不自动修复故障，也不替代运维人员做高风险决定。它的核心价值是：自动探查机器、组织诊断信息、减少命令记忆和重复劳动，并把完整上下文交给新人或资深运维判断。

本项目首先用于个人学习、秋招项目展示和面试讲解，不建设可直接投入生产的完整 GPU 运维平台。默认使用 Go 实现，通过 Mock 构造告警和故障机器，不接入真实公司内部系统。

项目成功标准：

1. 能在本地稳定演示从告警到只读调查报告的完整闭环。
2. 能清楚说明工作流、Agent 和人工决策的边界。
3. 能展示动态工具选择、结构化 Observation、证据链、安全约束和终止机制。
4. 项目作者能够解释主要架构选择及 trade-off。
5. 不为了显得完整而堆砌与面试主线无关的生产级功能。

## 2. 核心定位

面向新人和资深运维的 GPU 只读诊断助手，而不是自治运维或自动修复系统。

对新人，它帮助：

- 减少对诊断命令和排查顺序的记忆负担；
- 解释为什么执行某项检查；
- 区分已确认事实、推断和未知项；
- 在证据不足时携带完整上下文升级给资深运维。

对资深运维，它帮助：

- 自动完成标准化初检和重复信息采集；
- 排除常见原因并减少无关检查；
- 将原始日志和命令结果整理成结构化证据；
- 接手疑难问题时直接获得可审计的调查上下文。

面试中的一句话介绍：

> 我实现了一个面向 GPU 运维的受控只读诊断 Agent。它根据告警和机器当前状态逐步调用白名单诊断工具，自动完成原本需要人工登录机器执行的状态、进程、驱动和日志检查，并输出区分事实、推断、未知项和建议的证据化报告。Agent 没有修改机器状态的权限，所有恢复操作都由运维人员决定。

## 3. 为什么使用 Agent

### 3.1 不为了使用 Agent 而替代固定流程

如果告警类型、检查顺序和分支条件能够提前稳定穷举，确定性工作流比 LLM 更简单、可靠、可测试。常见故障优先匹配结构化 SOP，不让模型重新发明已知流程。

如果系统只是固定执行一组命令再拼接输出，它只是采集脚本。Agent 性体现在：面对模糊告警或不完整信息时，根据已有 Observation 判断当前缺少什么证据，在有限只读工具中选择下一项有价值的检查，并在证据充分、预算耗尽或无法继续判断时终止。

### 3.2 Agent 的有限职责

Agent 可以：

- 理解非标准化告警和当前诊断上下文；
- 关联多个结构化 Observation；
- 维护有限的候选原因；
- 在白名单工具中选择下一项只读检查；
- 解释为什么需要该检查；
- 汇总事实、推断、冲突信息和未知项；
- 证据不足时停止推测并升级人工。

Agent 不可以：

- 获得任意 Shell；
- 自行扩大工具、参数、主机或时间范围；
- 修改文件、配置、进程、驱动、GPU 或主机状态；
- 决定自己的权限、调用上限或终止规则；
- 把未经 Observation 支持的内容写成已确认事实；
- 执行终止进程、GPU reset、重载驱动、重启机器等恢复动作。

### 3.3 工作流、Agent 与人工的边界

```text
确定性代码：安全约束、工具执行、参数校验、状态管理和终止兜底
结构化 SOP：已知问题的必要检查步骤和明确分支
Agent：未知或不完整信息下的有限只读调查与解释
运维人员：复核报告、判断真实影响并决定所有状态变更操作
```

一个重要原则是：认知上的灵活性不等于操作权限。越是程序无法确定的情况，潜在风险越未知，因此 Agent 可以灵活分析，但不能获得更大的执行权限。

已经被反复验证且能够稳定描述的 Agent 调查路径，应逐步沉淀为确定性 SOP；Agent 继续处理尚未覆盖的长尾组合。

## 4. MVP 核心闭环

第一版只完成：

```text
Mock 告警
→ SOP Router 匹配已知流程，否则进入通用只读调查
→ 调用只读语义化工具
→ 从统一 Mock Machine State 获得 Observation
→ 更新 Diagnosis State 和 Evidence
→ SOP 或 Agent 选择下一项检查
→ 命中结论、证据不足或触发终止条件
→ 输出 Diagnosis Report
```

项目与普通问答助手的区别是：它不只根据用户提供的文字回答，还会主动读取机器状态、保存诊断进度、调用工具取得新证据、基于 Observation 推进调查，并输出可审计的运行结果。

项目与自治运维 Agent 的区别是：它只调查和建议，不改变机器状态。

## 5. 秋招 MVP 的技术重点

### 5.1 受控 Agent 循环

模型根据 Alert、历史 Observation、Evidence、当前候选原因和剩余预算选择下一步，而不是一次生成结论。循环必须能够解释：

- 上下文如何保存和裁剪；
- 工具结果如何结构化回填；
- 为什么选择下一项检查；
- 何时认为证据充分；
- 何时承认无法判断；
- 如何防止重复调用、无效探索和无限循环。

终止条件由外部程序强制执行，至少包括：得出有证据支持的结论、确认当前无法判断、达到最大步数、重复调用、工具连续失败或调查预算耗尽。

### 5.2 SOP 与 Agent 的混合架构

- 已知问题：确定性路由匹配结构化 SOP，SOP 规定必要步骤和明确分支；Agent 可负责解释，但不能改变强制步骤。
- 未知问题：先执行最小只读基线检查，再由 Agent 基于 Observation 选择有限的后续检查。
- 证据冲突或不足：整理已知上下文并升级人工，不强行输出根因。

面试重点是能够比较纯工作流、纯 LLM Agent 和混合架构：确定性流程保证已知路径的可靠性，Agent 降低长尾组合的流程编排成本，外部约束限制 Agent 的风险。

### 5.3 语义化只读诊断工具

Agent 和 SOP 不直接获得 Shell，而是调用有限的语义化工具，例如：

- `query_gpu_status`
- `query_gpu_processes`
- `query_gpu_topology`
- `query_driver_status`
- `query_xid_events`
- `query_host_resources`
- `query_recent_kernel_logs`

模型只能选择已注册工具及有限参数。工具层负责：

- 工具白名单和参数 Schema 校验；
- 限制目标主机、GPU、查询时间范围和输出大小；
- 超时、调用次数限制和错误分类；
- 将底层固定命令输出转换成结构化 Observation；
- 过滤敏感信息并记录审计信息；
- 在真实只读适配器与 Mock 实现之间复用统一接口。

底层未来可以执行经过审查的固定只读命令，但任意 Shell 和所谓“只读 Shell”都不属于 MVP。

### 5.4 统一 Mock Machine State

所有工具必须读取同一份场景状态，保证设备列表、温度、进程、驱动和日志等 Observation 彼此一致。Mock 不只是临时假数据，而是稳定演示、自动测试、复现诊断路径和评测 Agent 决策的基础设施。

### 5.5 确定性安全边界

安全限制必须在 Agent 外部执行，不能只依赖 Prompt。

MVP 只提供一类可执行能力：白名单内、只读、低风险的诊断检查。任何改变状态的动作都没有工具入口。

报告可以建议人工考虑以下动作，但系统不能执行：

- 终止进程；
- GPU reset；
- 重新加载驱动；
- 隔离节点或迁移任务；
- 重启机器；
- 修改配置、内核参数或固件。

建议必须同时说明依据、潜在影响、无法确认项和需要人工确认的内容，不能伪装成已执行结果。

### 5.6 证据链和诊断报告

最终输出不能只是模型生成的一段结论。每个关键事实和推断都应能追溯到具体工具调用和 Observation。

报告至少区分：

1. 已确认事实；
2. 基于事实的推断与置信程度；
3. 当前故障结论，若无法确认则明确说明；
4. 无法确认项和冲突信息；
5. 建议运维人员进行的下一步；
6. Evidence 与 Observation 引用。

模型负责组织和解释，程序负责校验证据引用是否存在，并禁止没有证据的内容进入“已确认事实”。

## 6. 推荐的最小架构

```text
CLI
 ↓
Diagnosis Orchestrator
├── SOP Router / Runner
├── Constrained LLM Planner
├── Read-only Tool Registry
├── Policy Checker
├── Diagnosis State / Loop Guard
└── Evidence Validator
 ↓
Unified Mock GPU Machine
 ↓
Diagnosis Report
 ↓
Human Operator
```

第一版优先使用 CLI 展示逐步调查过程，不因前端开发推迟核心闭环。

## 7. MVP 场景

第一版控制在五个可重复演示的场景：

1. 正常场景：验证系统不会为了给出答案而强行判断故障。
2. GPU 显存占用过高：发现高占用进程并形成证据链。
3. GPU 温度过高：结合温度、利用率和散热相关信息判断。
4. GPU Xid 错误或掉卡：结合设备状态、驱动信息和日志诊断。
5. 模糊或未知异常：根据不同现场 Observation 动态选择检查；证据不足时整理信息并升级人工。

只准备约 3 个结构化 SOP，其余场景进入通用只读调查或正常终止。至少有一个场景必须体现“相同或模糊告警，在不同 Mock 状态下选择不同调查路径”，否则项目容易退化为固定脚本。

MVP 不建设自然语言 SOP 自动转换平台。

## 8. 告警分级和业务影响边界

本项目面向出售虚机或 GPU 资源的边缘节点场景。基础设施平台通常不知道租户在虚机内运行什么业务，因此 MVP 不推测租户业务的重要性和实际业务损失。

MVP 最多保留 `info`、`warning`、`critical` 等技术等级，仅根据 GPU 和宿主机可观察事实辅助诊断入口，不建设完整告警管理平台。

必须区分：

```text
技术严重性：GPU 或宿主机故障本身有多严重
处理优先级：结合 SLA、客户保障等级和运营策略后的响应顺序
```

处理优先级应由外部告警或策略系统提供，不由诊断 Agent 猜测。客户分层、租户任务价值、告警风暴、跨节点关联和完整 SLA 调度不属于 MVP。

## 9. 明确不做的内容

除非后续确认直接影响核心闭环，否则 MVP 不实现：

- 真实生产机器和公司内部系统接入；
- 任意 Shell 或通用命令执行接口；
- 任何修改机器状态的工具；
- 自动恢复、审批后恢复或高风险操作执行；
- 完整告警平台、告警压缩和跨节点根因关联；
- 客户分层、合同 SLA 和租户业务影响判断；
- 完整 RAG 知识库和群聊知识沉淀平台；
- 自然语言 SOP 自动编译、审核和发布平台；
- 多 Agent 协作；
- 复杂 Web 前端、集群调度和生产级高可用。

这些内容可以作为面试中的生产化扩展边界，但不能抢占 MVP 开发时间。

## 10. 协作和讨论原则

后续分析、设计和实现都以“完成秋招项目，并能在面试中讲清楚”为主要判断标准。

讨论前优先判断：

- 它是否影响只读诊断闭环？
- 它能否说明 Agent 相比脚本或纯工作流的必要性？
- 它是否涉及安全边界、证据可信度或循环可靠性？
- 项目作者能否在几分钟内说明设计原因和 trade-off？
- 它值得实际实现，还是只需作为生产化边界了解？

讨论应帮助项目作者回答：

- 为什么这是 Agent，而不是命令采集脚本或普通问答助手？
- 为什么采用 SOP 与 Agent 混合架构？
- 为什么 Agent 只调查、不修复？
- LLM 拥有哪些决策权，哪些必须由程序控制？
- Alert、Tool、Observation、Diagnosis State、Evidence 和 Report 如何关联？
- Agent 循环如何终止并防止重复调用？
- 为什么不提供任意 Shell 或“只读 Shell”？
- 如何保证 Mock 中多个工具结果一致？
- 如何让结论绑定证据并减少幻觉？
- 如何评测工具选择、诊断路径和最终报告？
- 如果接入真实环境，还缺少哪些隔离、身份、审计和可靠性能力？

不要一次生成项目作者无法理解的完整工程。核心数据流、Agent 循环、安全边界和关键取舍先讨论清楚；达到“足以实现、足以解释、风险可控”后及时进入编码验证。

## 11. 当前阶段与后续推进顺序

### 11.1 已实现并验证的基础协议

只读诊断 MVP 的运行主干已经完成 Go 编码验证。以下协议已有实现和测试，不应在后续场景中无理由重新发明：

- Alert 与经过校验的 Scope 分离，第一版单 Target 由 Orchestrator 从 Scope 注入；
- PlannerDecision、ToolCall、Observation、ObservedFact、EvidenceRef 和 DiagnosisReport 的责任与 ID 关联；
- Observation 中强类型 Data、可引用 Facts 和受限 Raw 的三层结果；
- 显式受限的 FactValue、ObservationData 和工具专属参数类型；
- ToolCall/Observation 状态、固定错误码、调用指纹、有限重试和外部预算；
- DiagnosisState 的追加式历史、动态派生计数和生命周期；
- ConfirmedFinding、Inference、Unknown、Recommendation 的报告边界；
- 统一 Mock、确定性 Planner、Policy、Loop Guard、证据化报告和 CLI 依赖组装；
- 执行预算耗尽后禁止新工具调用，但仍允许 Planner 提交不执行工具的 `finish` 或 `escalate`；
- 固定 Clock 和 ID Generator 保证同一场景得到稳定路径、Facts 和报告。

详细字段、默认限制、Mock 数值和测试条件以 `docs/agent-runtime-and-diagnosis-state.md` 为唯一设计来源。`AGENTS.md` 只保留项目总纲与协作边界，避免复制出第二份容易漂移的协议。

### 11.2 已实现并验证的场景

当前已有两条可通过 CLI 重复运行的确定性调查路径。

高显存场景：

```text
gpu_abnormal
→ query_gpu_status
→ query_gpu_processes(GPU-0)
→ finish
```

它只确认 GPU-0 的高显存快照和 PID-4321 是本次观察中的主要直接占用来源，不确认内存泄漏。

Xid / 掉卡场景：

```text
gpu_abnormal
→ query_gpu_status
→ query_driver_status
→ query_xid_events(GPU-0)
→ query_recent_kernel_logs(GPU-0)
→ finish
```

它确认 GPU-0 unavailable、驱动/NVML 可用、Xid 79 和对应 NVRM 日志，只有限推断现象与 GPU-0 的主机通信丢失一致，不确认永久硬件损坏或具体物理根因。

当前 Tool Registry 只有五个只读语义化工具：

```text
query_gpu_status
query_gpu_processes
query_driver_status
query_xid_events
query_recent_kernel_logs
```

以下失败和安全分支已有自动测试：Scope 外 GPU、越界参数、重复调用、执行预算、连续策略拒绝、连续工具失败、无效 Planner 输出、驱动/NVML 不可用、Xid/日志为空、Xid 与日志不匹配、EvidenceRef 悬空和越权恢复声明。

运行入口：

```bash
go run ./cmd/gpu-agent --scenario high-memory
go run ./cmd/gpu-agent --scenario xid-drop
```

全量验证命令：

```bash
go test ./...
go test -race ./...
go vet ./...
```

### 11.3 当前明确未实现

不得把以下内容描述为已经完成：

- 高温、正常和未知异常场景；
- `query_gpu_topology`、`query_host_resources` 等其余只读工具；
- SOP Router 和结构化 SOP Runner；
- `PlannerContext` 的实际 JSON 裁剪与序列化；
- 真实 LLM Provider、结构化输出解析、重试和 LLM Planner；
- LLM Planner 与 Deterministic Planner 的对照评测；
- 真实 GPU、SSH、生产告警或生产日志接入。

### 11.4 新对话的下一项工作

新对话不要直接接入 LLM，也不要重新设计已经实现的运行协议。下一项工作是设计并实现第三条高温诊断切片，使同类模糊告警具有另一条有证据支持的调查路径。

建议顺序：

```text
先阅读 AGENTS.md 和 docs/agent-runtime-and-diagnosis-state.md
→ 检查当前 main、工作区和全量测试
→ 小范围设计高温场景的统一 Mock 字段、工具 Data、Facts 和报告边界
→ 判断复用 query_recent_kernel_logs 是否足够，或是否确有必要增加 query_host_resources
→ 先写模型/Mock 不变量测试
→ 实现高温 Deterministic Planner 分支和报告
→ 跑通 CLI 与端到端测试
→ 再设计最小 SOP Router，并复用同一 Tool、State、Evidence 和 Report 基础设施
```

高温切片不得把单次温度高直接写成硬件故障，也不得猜测租户业务影响。阈值仍只能标记为本地稳定演示规则。

### 11.5 何时接入 LLM

至少满足以下条件后再接入受限 LLM Planner：

1. 高显存、Xid/掉卡和高温三条确定性路径均通过测试；
2. 至少一个结构化 SOP 与通用调查路径复用相同工具和证据基础设施；
3. 同类模糊告警在不同 Mock State 下会产生不同、合理的调查路径；
4. `PlannerContext` 能只暴露 Scope 内、裁剪后的 Observation 和剩余预算；
5. 已有评测可以判断工具选择、重复调用、终止和报告是否合格。

接入时只新增一个实现现有 `Planner` 接口的 LLM Planner。Orchestrator、Policy、Scope、工具执行、预算、Fact 生成、Evidence 校验和报告安全边界继续由确定性程序控制。

编码阶段可以根据 Go 编译、序列化和测试结果微调字段名、文件拆分、构造函数名称和错误包装；如果必须改变安全边界、证据语义、Planner 权限、报告责任或既有验收路径，应停止编码并先重新讨论。
